# Reaction Roles Bot (V3)
Global Discord Reaction Roles Bot — Koyeb Ready

## 🚀 Deployment (Koyeb)
1. Upload this project to GitHub.
2. Connect your GitHub repo to Koyeb.
3. In Koyeb's settings:
   - **Build command:** `npm install`
   - **Start command:** `node index.js`
4. Add environment variables:
   - `BOT_TOKEN`: Your Discord bot token
   - `CLIENT_ID`: Your bot's application ID

## ⚙️ Features
- Global slash commands for Reaction Roles
- JSON-based storage (no database setup needed)
- AutoRole, Timed Roles, and Group Roles
- Customizable rotating status (configurable in `config.json`)

## 💡 Tips
- Edit `config.json` to change status messages or interval.
- Roles and messages are persisted automatically.

---
© 2025 YouWare — Reaction Roles Bot V3
